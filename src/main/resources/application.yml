spring:
  # PostgreSQL
  datasource:
    hikari:
      connectionTimeout: 20000
      maximum-pool-size: 5
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=qa_shopee
    username: postgres
    password: postgres
  # Flyway
  flyway:
    url: jdbc:postgresql://localhost:5432/postgres
    schemas: qa_shopee
    password: postgres
    user: postgres
    enabled: true
    #    validate-migration-naming: true
  # GraphQL
  graphql:
    graphiql:
      enabled: true
      path: /graphiql
# Logging
#logging:
#  level:
#    root: debug
mybatis:
  mapper-locations: classpath:src.main.resources.batis/*.xml
  type-aliases-package: com.shopee.shopeebeadmindemo.models.responses
#  config-location: classpath:src.main.resources.batis/mybatis-config.xml
#  mybatis.config-location=classpath:mybatis/mybatis-config.xml
#  mybatis.mapper-locations=classpath:mybatis/mapper/*.xml
#  mybatis.type-aliases-package=com.example.demo.model